#include <stdio.h>
/**
 * main - serves as the entry point of the program.
 * Return: By default returns zero if no errors otherwise return non-zero.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
