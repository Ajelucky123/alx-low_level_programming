#include <stdio.h>
/**
 * main - entry point of the program.
 *
 * Return: return 1 if success otherwise return non-zero if error.
 */
int main(void)
{
	write(2, "and that piece of art is useful\" - Dora Korpar, 2015-10-19\n", 61);
	return (1);
}
