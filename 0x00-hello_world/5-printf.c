#include <stdio.h>
/**
 * main-entry point for the program
 *
 * Return: By default return 0 if errors, return non-zero if no errors.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
