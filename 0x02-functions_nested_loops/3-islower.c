#include "main.h"
/**
 *  _islower - This program will check for lowercase character
 *@c: character to check for
 *  Return: Always 0 (Success).
 */
int _islower(int c);


{
	return (c > = 'a' && c < = 'z');
}
